server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/web?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    html5: true
    mode: HTML5
    servlet:
      content-type: text/html
